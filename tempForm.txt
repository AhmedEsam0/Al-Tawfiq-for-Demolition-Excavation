formSubmitted(form: NgForm) {
  console.log(form.value); // لو عايز تشوف البيانات

  // هنا بنمسك الـ HTML form الفعلي
  const formElement = form.form.get('')?.root?.value ? form.form : form;

  emailjs
    .sendForm(
      'service_7o7u43u',
      'template_sgfwfyo',
      form.form as any, // هنا المهم: الـ HTML FORM element
      {
        publicKey: 'yOIAzAxlvxcsr2S8a',
      }
    )
    .then(
      () => {
        console.log('SUCCESS!');
      },
      (error) => {
        console.log('FAILED...', (error as EmailJSResponseStatus).text);
      }
    );
}
